package us.greatapps4you.kotlin.essential.training.funcolio

fun debuggingConcepts() {
    val a = 10
    val b = 20
    val c = a + b // 🔴 Click the margin here in your IDE to set a BREAKPOINT.
    // When running in Debug mode, execution pauses here.
    // "Step Over" (F8) goes to the next line.
    // "Step Into" (F7) goes inside the function being called.
    // "Evaluate Expression" (Alt+F8) lets you run math or code live while paused.
}